package com.gpu.rentaler.sys.repository;

import com.gpu.rentaler.sys.model.GPURantals;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GPURantalsRepository extends JpaRepository<GPURantals, Long> {
}
